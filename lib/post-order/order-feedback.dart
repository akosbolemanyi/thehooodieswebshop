import 'package:flutter/material.dart';

class OrderFeedback extends StatelessWidget {
  const OrderFeedback({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is the order-feedback page.'),
      ),
    );
  }
}
